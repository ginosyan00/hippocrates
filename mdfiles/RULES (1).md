# ПРАВИЛА ПРОЕКТА — ЧИТАТЬ ПЕРВЫМ!

> ⚠️ **ДЛЯ AI**: Это **ГЛАВНЫЙ** файл. Читай его **КАЖДЫЙ раз** в новом чате.  
> Все правила здесь. Следуй им строго без исключений.

---

## 📚 ОБЯЗАТЕЛЬНЫЕ ДОКУМЕНТЫ (читай по порядку)

### При начале нового чата читай ВСЕ файлы в этом порядке:

1. ✅ **RULES.md** (этот файл) — главные правила и запреты
2. ✅ **ENVIRONMENT.md** — что УЖЕ установлено (не переустанавливай!)
3. ✅ **ARCHITECTURE.md** — где находятся модули
4. ✅ **CODING-RULES.md** — как писать код
5. ✅ **PROGRESS.md** — что уже сделано, что делать дальше

### При работе с deployment:
6. **DEPLOYMENT.md** — как деплоить на сервер

### Для понимания проекта:
7. **TECHNICAL-SPEC.md** — полное техническое задание
8. **PLAN.md** — план разработки

---

## 🚫 ЗАПРЕЩЕНО (НИКОГДА НЕ ДЕЛАЙ!)

### Инфраструктура:
- ❌ **Не используй** SQL БД (PostgreSQL, MySQL) вместо MongoDB
- ❌ **Не переустанавливай** Node.js, MongoDB если они уже есть
- ❌ **Не используй** SQLite или in-memory БД
- ❌ **Не меняй** мажорные версии пакетов без согласования
- ❌ **Не создавай** локальные файлы для хранения данных

### Код и архитектура:
- ❌ **Не используй** `any` в TypeScript
- ❌ **Не пиши** бизнес-логику в контроллерах (только в services)
- ❌ **Не дублируй** код
- ❌ **Не создавай** функции длиннее 50 строк
- ❌ **Не создавай** файлы длиннее 400 строк
- ❌ **Не пиши** прямые запросы к MongoDB (только через Prisma)

### Безопасность и Multi-tenancy:
- ❌ **НЕ ЗАБЫВАЙ** фильтровать по `clinicId` во ВСЕХ запросах
- ❌ **Не коммитить** секреты, API ключи, пароли
- ❌ **Не коммитить** файлы `.env`
- ❌ **Не коммитить** закомментированный код
- ❌ **Не логировать** пароли, токены, личные данные

### Стили (если используются):
- ❌ **Не используй** произвольные цвета (`bg-blue-500`)
- ❌ **Не пиши** inline styles (`style={{ ... }}`)
- ❌ **Не используй** произвольные spacing (`p-[13px]`)

---

## ✅ ОБЯЗАТЕЛЬНО (ВСЕГДА ДЕЛАЙ!)

### Перед написанием кода:
- ✅ **Прочитай** ENVIRONMENT.md — что можно использовать
- ✅ **Прочитай** ARCHITECTURE.md — куда писать код
- ✅ **Прочитай** CODING-RULES.md — как писать код
- ✅ **Используй** TypeScript strict mode
- ✅ **Используй** только Prisma ORM для БД

### При написании кода:
- ✅ **Следуй** naming conventions из CODING-RULES.md
- ✅ **Создавай** типы TypeScript для всех данных
- ✅ **Пиши** маленькие функции (< 50 строк)
- ✅ **Пиши** маленькие файлы (< 400 строк)
- ✅ **Валидируй** все входные данные (Joi)
- ✅ **Обрабатывай** ошибки правильно (try/catch)
- ✅ **Фильтруй по clinicId** во ВСЕХ запросах к БД

### Multi-tenancy (КРИТИЧНО!):
- ✅ **ВСЕГДА** добавляй `clinicId` к фильтрам запросов
- ✅ **ВСЕГДА** проверяй что данные принадлежат текущей клинике
- ✅ **ВСЕГДА** используй middleware для tenant isolation
- ✅ **Пиши тесты** на tenant isolation

### После написания кода:
- ✅ **Проверь** TypeScript ошибки (0 errors)
- ✅ **Проверь** ESLint warnings (0 warnings)
- ✅ **Обнови** PROGRESS.md (отметь задачу)
- ✅ **Напиши тесты** для критичной логики
- ✅ **Протестируй** что всё работает

---

## 🎯 WORKFLOW ДЛЯ AI

### При начале нового чата:

```
ОБЯЗАТЕЛЬНАЯ ПОСЛЕДОВАТЕЛЬНОСТЬ:

1. Прочитай RULES.md (этот файл)
   └─> Запомни все запреты и обязательства

2. Прочитай ENVIRONMENT.md
   └─> Узнай что УЖЕ есть (не переустанавливай!)

3. Прочитай ARCHITECTURE.md
   └─> Пойми структуру проекта

4. Прочитай CODING-RULES.md
   └─> Запомни как писать код

5. Прочитай PROGRESS.md
   └─> Узнай что сделано, что делать

ТОЛЬКО ПОСЛЕ ЭТОГО начинай работу!
```

### При работе над задачей:

```
1. Проверь ENVIRONMENT.md
   └─> Что можно/нельзя использовать

2. Проверь ARCHITECTURE.md
   └─> Где писать код

3. Проверь CODING-RULES.md
   └─> Как писать код

4. Напиши код
   └─> Следуя всем правилам

5. Обнови PROGRESS.md
   └─> Отметь задачу выполненной
```

---

## 📐 АРХИТЕКТУРА (кратко)

**Полная информация в ARCHITECTURE.md**

### Структура проекта:
```
hippocrates/
├── frontend/        # React SPA (TypeScript + Vite + Tailwind)
│   ├── src/
│   │   ├── components/    # UI компоненты
│   │   ├── pages/         # Страницы
│   │   ├── services/      # API clients
│   │   ├── store/         # Zustand state
│   │   └── utils/         # Utilities
│   └── package.json
│
├── backend/         # Express API (JavaScript + Prisma + MongoDB)
│   ├── src/
│   │   ├── controllers/   # Контроллеры (тонкие)
│   │   ├── services/      # Бизнес-логика (толстые)
│   │   ├── middlewares/   # Auth, validation, tenant
│   │   ├── routes/        # API routes
│   │   └── config/        # Configuration
│   ├── prisma/
│   │   └── schema.prisma  # DB schema
│   └── package.json
│
└── Documentation/   # Документация (ты здесь)
```

### Правила:
- **Backend:** Controllers — тонкие, Services — вся логика
- **Frontend:** Components — UI, Services — API calls
- **Prisma:** ЕДИНСТВЕННЫЙ способ работы с БД
- **Multi-tenancy:** Фильтр по `clinicId` ВЕЗДЕ

---

## 💻 CODING (кратко)

**Полная информация в CODING-RULES.md**

### Backend (Express + Prisma):
```javascript
// ✅ ПРАВИЛЬНО
// controllers/patient.controller.js
async function getPatients(req, res) {
  const { clinicId } = req.user; // из JWT
  const patients = await patientService.findAll(clinicId);
  res.json({ success: true, data: patients });
}

// services/patient.service.js
async function findAll(clinicId) {
  return await prisma.patient.findMany({
    where: { clinicId }, // ОБЯЗАТЕЛЬНО!
  });
}

// ❌ НЕПРАВИЛЬНО
async function getPatients(req, res) {
  // БЕЗ clinicId фильтра — вернёт данные ВСЕХ клиник!
  const patients = await prisma.patient.findMany();
  res.json(patients);
}
```

### Frontend (React + TypeScript):
```typescript
// ✅ ПРАВИЛЬНО
interface Patient {
  id: string;
  name: string;
  phone: string;
}

const PatientList: React.FC = () => {
  const { data, isLoading } = useQuery<Patient[]>('patients', fetchPatients);
  
  if (isLoading) return <Spinner />;
  
  return (
    <div className="space-y-4">
      {data?.map(patient => (
        <PatientCard key={patient.id} patient={patient} />
      ))}
    </div>
  );
};

// ❌ НЕПРАВИЛЬНО
const PatientList = () => {
  const [data, setData] = useState<any>(); // ❌ any!
  // ...
};
```

---

## 🔒 MULTI-TENANCY (КРИТИЧНО!)

### Главное правило:

**КАЖДЫЙ запрос к БД ДОЛЖЕН фильтроваться по `clinicId`!**

### Middleware для автоматической фильтрации:

```javascript
// middlewares/tenant.middleware.js
function tenantMiddleware(req, res, next) {
  const { clinicId } = req.user; // из JWT
  
  // Добавляем clinicId к каждому запросу
  req.tenantFilter = { clinicId };
  
  next();
}

// Использование
app.use('/api', authenticate, tenantMiddleware);
```

### В Services:

```javascript
// ✅ ПРАВИЛЬНО — используем req.tenantFilter
async function findAll(req) {
  return await prisma.patient.findMany({
    where: {
      ...req.tenantFilter, // { clinicId: "..." }
      // остальные фильтры
    }
  });
}

// ❌ НЕПРАВИЛЬНО — НЕТ clinicId
async function findAll() {
  return await prisma.patient.findMany(); // ОПАСНО!
}
```

### Тесты на tenant isolation:

```javascript
// __tests__/tenant-isolation.test.js
test('should not return data from other clinics', async () => {
  const clinic1Token = generateToken({ clinicId: 'clinic1' });
  const clinic2Token = generateToken({ clinicId: 'clinic2' });
  
  // Создаём пациента в clinic2
  await createPatient({ clinicId: 'clinic2', name: 'John' });
  
  // Запрашиваем пациентов из clinic1
  const res = await request(app)
    .get('/api/patients')
    .set('Authorization', `Bearer ${clinic1Token}`);
  
  // Не должен вернуть John из clinic2
  expect(res.body.data).not.toContainEqual(
    expect.objectContaining({ name: 'John' })
  );
});
```

---

## 🔄 ОБНОВЛЕНИЕ ДОКУМЕНТАЦИИ

### Когда обновлять:

| Событие | Файл | Кто |
|---------|------|-----|
| Завершил задачу | `PROGRESS.md` | AI/Developer (ОБЯЗАТЕЛЬНО!) |
| Добавил модуль | `ARCHITECTURE.md` | Developer |
| Изменил правила кода | `CODING-RULES.md` | Tech Lead |
| Добавил инструмент | `ENVIRONMENT.md` | DevOps |
| Изменил деплой | `DEPLOYMENT.md` | DevOps |

### Как обновлять PROGRESS.md:

```markdown
## ✅ ВЫПОЛНЕНО
- [x] Backend: API /auth/register
- [x] Backend: API /auth/login
- [x] Frontend: Login страница  ✅ НОВОЕ

## 🔄 В РАБОТЕ  
- [ ] Backend: API /patients (50%) ⬅️ ОБНОВЛЕНО

## 📋 СЛЕДУЮЩИЕ ЗАДАЧИ
- [ ] Backend: API /appointments
- [ ] Frontend: Dashboard layout
```

---

## ✅ CHECKLIST ДЛЯ AI

### Перед началом работы:
- [ ] Прочитал RULES.md (этот файл)
- [ ] Прочитал ENVIRONMENT.md (что уже есть)
- [ ] Прочитал ARCHITECTURE.md (структура)
- [ ] Прочитал CODING-RULES.md (стандарты)
- [ ] Прочитал PROGRESS.md (прогресс)
- [ ] Понял текущую задачу
- [ ] Знаю где писать код
- [ ] Знаю как писать код

### После завершения задачи:
- [ ] Код следует CODING-RULES.md
- [ ] Используется Prisma для БД
- [ ] ОБЯЗАТЕЛЬНАЯ фильтрация по `clinicId`
- [ ] Нет TypeScript ошибок
- [ ] Нет ESLint warnings
- [ ] Нет дублирования кода
- [ ] Нет `any` в TypeScript
- [ ] Функции < 50 строк
- [ ] Файлы < 400 строк
- [ ] Написаны тесты (если нужно)
- [ ] Обновил PROGRESS.md ✅
- [ ] Всё работает ✅

---

## 🎯 ЦЕЛЬ ПРОЕКТА

**Создать SaaS-платформу для стоматологических клиник с multi-tenancy архитектурой.**

**MVP включает:**
- ✅ Регистрация клиник и авторизация
- ✅ Управление сотрудниками, пациентами, приёмами
- ✅ Публичные лендинги клиник
- ✅ Онлайн-запись пациентов
- ✅ Календарь и расписание врачей
- ✅ Полная изоляция данных между клиниками

**Технологии:**
- Frontend: React 18 + TypeScript + Vite + Tailwind
- Backend: Node.js 20 + Express + JavaScript
- Database: MongoDB (Prisma ORM)
- Auth: JWT + bcrypt
- Hosting: Render (backend) + Vercel (frontend)

**Результат:** 
Работающая SaaS-платформа с многопользовательским доступом и строгой изоляцией данных.

---

## 🚨 КРИТИЧНЫЕ МОМЕНТЫ

### 1. Multi-tenancy — ВСЕГДА фильтруй по clinicId!

```javascript
// ❌ ОПАСНО — вернёт данные ВСЕХ клиник
await prisma.patient.findMany();

// ✅ БЕЗОПАСНО — только текущей клиники
await prisma.patient.findMany({
  where: { clinicId: req.user.clinicId }
});
```

### 2. Не храни бизнес-логику в контроллерах!

```javascript
// ❌ НЕПРАВИЛЬНО
// controllers/patient.controller.js
async function create(req, res) {
  const patient = await prisma.patient.create({
    data: { ...req.body, clinicId: req.user.clinicId }
  });
  res.json(patient);
}

// ✅ ПРАВИЛЬНО
// controllers/patient.controller.js
async function create(req, res) {
  const patient = await patientService.create(req.user.clinicId, req.body);
  res.json({ success: true, data: patient });
}

// services/patient.service.js
async function create(clinicId, data) {
  // Здесь валидация, проверки, бизнес-логика
  return await prisma.patient.create({
    data: { ...data, clinicId }
  });
}
```

### 3. Валидируй ВСЕ входные данные!

```javascript
// middlewares/validation.middleware.js
const Joi = require('joi');

const createPatientSchema = Joi.object({
  name: Joi.string().min(2).max(100).required(),
  phone: Joi.string().pattern(/^\+374\s?\d{2}\s?\d{6}$/).required(),
  email: Joi.string().email().optional(),
});

function validate(schema) {
  return (req, res, next) => {
    const { error } = schema.validate(req.body);
    if (error) {
      return res.status(400).json({
        success: false,
        error: {
          code: 'VALIDATION_ERROR',
          message: error.details[0].message
        }
      });
    }
    next();
  };
}

// routes/patient.routes.js
router.post('/', validate(createPatientSchema), patientController.create);
```

---

**Последнее обновление:** 11.11.2025  
**Версия:** 1.0  
**Следующий review:** При появлении новых правил или изменении процессов

