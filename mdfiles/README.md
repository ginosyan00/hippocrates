# 🦷 Hippocrates Dental — SaaS-платформа для стоматологических клиник

> Облачная CRM-система с multi-tenancy и публичными лендингами для онлайн-записи пациентов.

---

## 🎯 ЧТО ЭТО?

**Hippocrates Dental** — это SaaS-платформа, объединяющая:

1. **Админ-панель (Dashboard)** — управление клиникой, врачами, пациентами, расписанием
2. **Публичный веб-сайт** — каталог клиник + индивидуальные лендинги для онлайн-записи

**Для кого:**
- Малые и средние стоматологические клиники (1-10 врачей)
- Администраторы и врачи
- Пациенты (онлайн-запись без звонков)

---

## 🚀 БЫСТРЫЙ СТАРТ

### Требования

- Node.js 20 LTS
- MongoDB 7.0+ (или MongoDB Atlas)
- npm или yarn

### Установка и запуск

```bash
# 1. Клонировать репозиторий
git clone https://github.com/your-org/hippocrates.git
cd hippocrates

# 2. Backend
cd backend
npm install
cp .env.example .env
# Отредактировать .env (DATABASE_URL, JWT_SECRET)
npx prisma generate
npx prisma db push
npm run dev
# Backend: http://localhost:5000

# 3. Frontend (в новом терминале)
cd ../frontend
npm install
npm run dev
# Frontend: http://localhost:5173
```

### Первый вход

После запуска откройте `http://localhost:5173` и зарегистрируйте первую клинику.

---

## 🏗️ ТЕХНОЛОГИИ

### Frontend
- **React 18.3** + TypeScript
- **Vite** (быстрая сборка)
- **Tailwind CSS** (стили)
- **React Query** (API queries)
- **Zustand** (state management)
- **React Router** (маршрутизация)

### Backend
- **Node.js 20** + Express.js
- **JavaScript** (ES6 modules)
- **Prisma ORM** (database)
- **MongoDB** (база данных)
- **JWT** (аутентификация)
- **bcrypt** (хеширование паролей)

### Инфраструктура
- **MongoDB Atlas** (БД в облаке)
- **Render** (backend hosting)
- **Vercel** (frontend hosting)

---

## 📁 СТРУКТУРА ПРОЕКТА

```
hippocrates/
├── frontend/           # React SPA
│   ├── src/
│   │   ├── components/    # UI компоненты
│   │   ├── pages/         # Страницы
│   │   ├── services/      # API clients
│   │   ├── store/         # Zustand stores
│   │   └── utils/         # Utilities
│   └── package.json
│
├── backend/            # Express API
│   ├── src/
│   │   ├── controllers/   # Контроллеры
│   │   ├── services/      # Бизнес-логика
│   │   ├── models/        # Prisma models
│   │   ├── middlewares/   # Middleware
│   │   ├── routes/        # Маршруты API
│   │   └── config/        # Конфигурация
│   ├── prisma/
│   │   └── schema.prisma  # DB schema
│   └── package.json
│
├── Documentation/      # 📚 Документация
│   ├── README.md              (этот файл)
│   ├── TECHNICAL-SPEC.md      (полное ТЗ)
│   ├── RULES.md               🔥 (главный для AI)
│   ├── ARCHITECTURE.md        (архитектура)
│   ├── CODING-RULES.md        (стандарты)
│   ├── PLAN.md                (план разработки)
│   ├── PROGRESS.md            (прогресс)
│   └── DEPLOYMENT.md          (деплой)
│
└── .cursor/
    └── rules/
        └── rules.mdc           (для Cursor AI)
```

---

## 📚 ДОКУМЕНТАЦИЯ

### 🔥 Для AI и разработчиков (читать в порядке):

1. **[RULES.md](RULES.md)** 🔥🔥🔥 — **ГЛАВНЫЙ** файл с правилами
2. **[ENVIRONMENT.md](ENVIRONMENT.md)** 🔥 — Что установлено, запреты
3. **[ARCHITECTURE.md](ARCHITECTURE.md)** — Структура проекта
4. **[CODING-RULES.md](CODING-RULES.md)** — Стандарты кодирования
5. **[PROGRESS.md](PROGRESS.md)** — Текущий прогресс

### Для понимания проекта:

6. **[TECHNICAL-SPEC.md](../TECHNICAL-SPEC.md)** — Полное техническое задание
7. **[PLAN.md](PLAN.md)** — План разработки по этапам

### Для деплоя:

8. **[DEPLOYMENT.md](DEPLOYMENT.md)** — Инструкции по деплою
9. **[env.example.txt](env.example.txt)** — Пример конфигурации

---

## 🛠️ КОМАНДЫ

### Development

```bash
# Backend
cd backend
npm run dev          # Запустить dev сервер
npm run build        # Собрать production
npm test             # Запустить тесты
npm run lint         # Проверить код

# Frontend
cd frontend
npm run dev          # Запустить dev сервер (Vite)
npm run build        # Собрать production
npm run preview      # Превью production build
npm test             # Запустить тесты
```

### Database

```bash
cd backend

# Создать миграцию (development)
npx prisma migrate dev --name add_new_field

# Применить миграции (production)
npx prisma migrate deploy

# Сгенерировать Prisma Client
npx prisma generate

# Открыть Prisma Studio (GUI для БД)
npx prisma studio

# Заполнить тестовыми данными
npm run db:seed
```

---

## 🔑 ОСНОВНЫЕ ФИЧИ (MVP)

### Dashboard (Админ-панель)

✅ Авторизация и регистрация клиники  
✅ CRUD сотрудников (врачи, ассистенты)  
✅ CRUD пациентов  
✅ CRUD приёмов (appointments)  
✅ Календарь расписания врачей  
✅ Настройки клиники (контакты, логотип, график)  
✅ Статистика приёмов  

### Public Website

✅ Главная страница Hippocrates  
✅ Каталог клиник с фильтром по городу  
✅ Индивидуальный лендинг клиники  
✅ Форма онлайн-записи к врачу  
✅ Адаптивный дизайн (mobile-first)  

### Multi-tenancy

✅ Полная изоляция данных по клиникам  
✅ Каждая клиника видит только свои данные  
✅ Защита от утечек между арендаторами  

---

## 👥 РОЛИ ПОЛЬЗОВАТЕЛЕЙ

| Роль | Описание | Доступ |
|------|----------|--------|
| **Admin** | Администратор клиники | Полный доступ к клинике |
| **Doctor** | Врач | Просмотр своих пациентов и расписания |
| **Assistant** | Ассистент | Помощь с административными задачами |
| **Patient** | Пациент | Только запись через сайт (без авторизации) |

---

## 🔐 БЕЗОПАСНОСТЬ

- **JWT аутентификация** (HS256, 7 дней)
- **bcrypt** для паролей (saltRounds=12)
- **HTTPS обязательно** (TLS 1.3)
- **Multi-tenancy isolation** (строгая изоляция данных)
- **Rate limiting** (100 req/min/IP)
- **CORS whitelist**
- **Validation** всех входных данных (Joi)

---

## 📊 НЕФУНКЦИОНАЛЬНЫЕ ТРЕБОВАНИЯ

| Метрика | Значение |
|---------|----------|
| API Response Time (p95) | ≤ 500 ms |
| Page Load (LCP) | ≤ 2.5 sec |
| Uptime (SLA) | ≥ 99.5% |
| Максимум клиник (MVP) | ≤ 100 |
| Одновременных пользователей | ≤ 500 |

---

## 🧪 ТЕСТИРОВАНИЕ

```bash
# Unit тесты
npm test

# E2E тесты
npm run test:e2e

# Coverage
npm run test:coverage
```

**Target:** ≥ 70% coverage для бизнес-логики

---

## 🚀 ДЕПЛОЙ

### Production

**Рекомендуемые платформы:**
- Backend: **Render** ($7/мес Starter)
- Frontend: **Vercel** (Free tier)
- Database: **MongoDB Atlas** (M0 Free tier)

**Инструкции:** См. [DEPLOYMENT.md](DEPLOYMENT.md)

---

## 📈 ROADMAP

### v1.0 (MVP) — Сейчас
- ✅ Базовая CRM для клиник
- ✅ Публичные лендинги
- ✅ Онлайн-запись пациентов

### v1.1 (1 месяц)
- 📧 Email/SMS уведомления
- 📄 История лечения пациента
- 📤 Экспорт данных (Excel, PDF)

### v1.2 (3 месяца)
- 💳 Онлайн-оплата
- 🌐 Многоязычность (AM/RU/EN)
- 📊 Расширенная статистика

### v1.3 (6 месяцев)
- 📱 Мобильное приложение
- 🎥 Онлайн-консультации
- 📦 Складской учёт

---

## 🐛 TROUBLESHOOTING

### Backend не запускается

```bash
# Проверить MongoDB
mongosh "mongodb://localhost:27017"

# Проверить .env
cat backend/.env

# Пересоздать Prisma Client
cd backend
npx prisma generate
```

### Frontend не видит API

```bash
# Проверить VITE_API_URL в frontend/.env
echo $VITE_API_URL  # должен быть http://localhost:5000

# Проверить CORS в backend
# Убедись что в backend/.env:
CORS_ORIGIN=http://localhost:5173
```

### Ошибки TypeScript

```bash
# Frontend
cd frontend
npm run type-check

# Исправить автоматически
npm run lint -- --fix
```

---

## 🤝 CONTRIBUTING

1. Прочитай [RULES.md](RULES.md) 🔥
2. Прочитай [CODING-RULES.md](CODING-RULES.md)
3. Создай feature branch
4. Сделай изменения
5. Напиши тесты
6. Отправь Pull Request
7. Обнови [PROGRESS.md](PROGRESS.md)

---

## 📞 КОНТАКТЫ

**Email:** info@hippocrates.am  
**Website:** https://hippocrates.am  
**Support:** support@hippocrates.am

---

## 📄 ЛИЦЕНЗИЯ

Proprietary — Все права защищены.

---

**Версия:** 1.0.0 (MVP)  
**Дата:** 11.11.2025  
**Статус:** 🚧 В разработке

---

**Команда:**
- Product Owner: Neetreino Specialist
- Lead Developer: [TBD]
- AI Assistant: Claude (Anthropic)

