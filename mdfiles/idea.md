# 🦷 Hippocrates Dental — Исходная идея проекта

> ⚠️ **ВНИМАНИЕ:** Это исходная идея проекта.  
> **Актуальное техническое задание:** См. [TECHNICAL-SPEC.md](TECHNICAL-SPEC.md) (создано 11.11.2025)  
> **Документация:** См. [Documentation/](Documentation/)

---

Абсолютно правильно. Сейчас я подготовлю **завершённое ТЗ**, где сначала подробно описывается **сам продукт — его концепция, назначение, аудитория, пользовательская логика**, а затем следует **техническая часть**, по которой можно сразу приступать к разработке.

Текст рассчитан на человека, который впервые слышит об идее, поэтому он содержит полное описание продукта, бизнес-ценности и функционала до мелочей, а затем — структурированный план реализации (архитектура, API, БД, стек, сроки).

---

# 🦷 ТЕХНИЧЕСКОЕ ЗАДАНИЕ (Черновик)

**Проект:** SaaS-платформа *Hippocrates Dental*
**Версия:** MVP
**Дата:** 11.11.2025
**Документ подготовлен для:** команды разработчиков
**Ответственный:** Product Owner — Neetreino Specialist

---

## 0. Описание продукта

### 0.1. Концепция

**Hippocrates Dental** — это SaaS-платформа для стоматологических клиник, объединяющая два компонента:

1. **Внутренняя CRM-система (Dashboard)** — веб-приложение для управления врачами, пациентами, расписанием и приёмами.
2. **Публичный веб-сайт** — платформа с каталогом клиник и индивидуальной страницей (лендингом) каждой клиники, где клиенты могут записываться на приём онлайн.

Hippocrates — это не просто CRM. Это экосистема, где каждая клиника имеет собственный кабинет и публичное онлайн-представительство в рамках общего бренда.

---

### 0.2. Проблема, которую решает продукт

Сегодня большинство стоматологических клиник используют разрозненные решения — блокноты, Excel, или устаревшие программы на локальных компьютерах. Это приводит к:

* потере информации о клиентах,
* неэффективному управлению расписанием врачей,
* отсутствию онлайн-записи для пациентов,
* отсутствию цифрового имиджа клиники (нет сайта, плохая видимость в интернете).

Hippocrates решает все эти проблемы централизованно:

* клиника ведёт всю работу в единой облачной системе;
* пациенты записываются онлайн, без звонков и ожидания;
* все данные защищены, разнесены по арендаторам (multi-tenancy).

---

### 0.3. Целевая аудитория

1. **Малые и средние стоматологические клиники (1–10 врачей)**, которые не имеют собственного IT-отдела.
2. **Администраторы клиник**, которые ведут записи и координируют работу врачей.
3. **Пациенты**, желающие быстро записаться на приём онлайн, без звонков и очередей.

---

### 0.4. Пользовательские роли

| Роль                              | Описание                                                  | Доступ                               |
| --------------------------------- | --------------------------------------------------------- | ------------------------------------ |
| **Admin (администратор клиники)** | Управляет клиникой, сотрудниками, расписанием, пациентами | Полный доступ в рамках своего tenant |
| **Doctor (врач)**                 | Просматривает собственное расписание и записи             | Доступ к своим пациентам             |
| **Assistant**                     | Помогает администратору с амминистративными задачами      | Ограниченный доступ                  |
| **Patient (пациент)**             | Посетитель сайта, может записаться на приём               | Только через публичную часть сайта   |

---

### 0.5. Принцип работы системы

**1. Администратор** регистрирует клинику в системе и получает доступ в личный кабинет.
**2. Добавляет сотрудников, врачей и базу пациентов.**
**3. Пациент** заходит на сайт hippocrates.am, выбирает клинику → врача → время → заполняет данные → отправляет заявку.
**4. Клиника получает уведомление и подтверждает приём в Dashboard.**
**5. Врач видит свой обновлённый календарь в реальном времени.**

Таким образом, весь цикл — от онлайн-заявки до подтверждения — проходит в одной системе.

---

### 0.6. Конкурентные преимущества

* SaaS-модель — не требует установки, доступен из браузера.
* Простая, интуитивная админ-панель.
* Многоарендность — каждая клиника изолирована от других.
* Индивидуальные лендинги под брендом Hippocrates (готовое SEO-решение).
* Поддержка на этапе внедрения (админ-интерфейс максимально прост).

---

### 0.7. Цели MVP-версии

MVP должен показать:

* Работоспособность SaaS-модели (multi-tenancy).
* Основной сценарий: пациент записывается онлайн, клиника подтверждает приём.
* Простую CRM-часть: пользователи, пациенты, приёмы, календарь.
* Лендинги клиник, доступные публично.

---

## 1. Функциональные требования

### 1.1. Компонент 1 — Админ-панель клиники (Dashboard)

Функциональность:

* Авторизация и регистрация админа.
* Просмотр, добавление, редактирование сотрудников.
* Ведение списка пациентов.
* Создание и управление приёмами (appointments).
* Просмотр расписания в виде календаря.
* Настройки клиники (контакты, описание, логотип, график).

### 1.2. Компонент 2 — Публичный сайт (Web)

Функциональность:

* Главная страница с описанием сервиса Hippocrates.
* Список клиник с фильтром по городу.
* Страница клиники (лендинг) с:

  * описанием, контактами, часами работы,
  * формой онлайн-записи.
* Отправка заявки (создание записи в БД).

### 1.3. Multi-tenancy

* Изоляция данных по полю `clinicId`.
* Все пользователи, пациенты и записи привязаны к конкретной клинике.

---

## 2. Нефункциональные требования

| Параметр                          | Значение                |
| --------------------------------- | ----------------------- |
| Среднее время ответа (p95)        | ≤ 500 мс                |
| Загрузка страницы (LCP)           | ≤ 2.5 сек               |
| Uptime                            | ≥ 99.5%                 |
| Количество арендаторов (тенантов) | ≤ 100                   |
| Одновременные пользователи        | ≤ 500                   |
| Ошибки API                        | ≤ 0.1% в неделю         |
| Безопасность                      | OWASP ASVS Level 2      |
| Авторизация                       | JWT (HS256, exp 7 дней) |

---

## 3. Технологический стек

### Frontend

* React + TypeScript
* Vite
* Tailwind CSS
* React Query
* Zustand (глобальное состояние)
* React Router
* Axios (HTTP client)

### Backend

* Node.js (Express.js)
* JavaScript (ES6 modules)
* Prisma ORM
* MongoDB (Atlas)
* JWT, bcrypt, Helmet, Joi

### Инфраструктура

* MongoDB Atlas — база данных
* Render — backend
* Vercel — frontend
* GitHub Actions — CI/CD

---

## 4. Архитектура проекта

* **Архитектура:** монорепозиторий (`/frontend` и `/backend`).
* **Связь между компонентами:** REST API.
* **Multi-tenancy:** tenantId в каждом документе.
* **Frontend** — SPA, взаимодействует с API через Axios.
* **Backend** — REST API на Express.
* **ORM:** Prisma (Mongo provider).

Структура:

```
apps/
  frontend/
    src/
      components/
      pages/
  backend/
    src/
      controllers/
      routes/
      models/
      middlewares/
```

---

## 5. Модель данных

| Сущность    | Поля                                                       | Описание             |
| ----------- | ---------------------------------------------------------- | -------------------- |
| Clinic      | id, name, slug, contactInfo, about, logo, workingHours     | Данные о клинике     |
| User        | id, clinicId, name, email, passwordHash, role              | Пользователь системы |
| Patient     | id, clinicId, name, phone, notes                           | Пациент              |
| Appointment | id, clinicId, doctorId, patientId, status, appointmentTime | Приём                |
| ClinicPage  | id, clinicId, about, logoUrl, address, workingHours        | Лендинг клиники      |

---

## 6. API (основные маршруты)

| Метод | Endpoint               | Назначение                 |
| ----- | ---------------------- | -------------------------- |
| POST  | /api/auth/register     | Регистрация клиники        |
| POST  | /api/auth/login        | Авторизация                |
| GET   | /api/clinic/me         | Данные клиники             |
| PUT   | /api/clinic/me         | Обновление данных клиники  |
| GET   | /api/users             | Список сотрудников         |
| POST  | /api/users             | Добавление сотрудника      |
| GET   | /api/patients          | Список пациентов           |
| POST  | /api/patients          | Добавление пациента        |
| GET   | /api/appointments      | Список приёмов             |
| POST  | /api/appointments      | Создание записи            |
| PATCH | /api/appointments/:id  | Изменение статуса          |
| GET   | /api/clinic-page/:slug | Публичная страница клиники |

---

## 7. Пользовательские сценарии

### Пациент

1. Заходит на сайт Hippocrates.
2. Выбирает клинику.
3. Открывает её страницу.
4. Заполняет форму записи.
5. Получает уведомление о приёме.

### Администратор клиники

1. Логинится в Dashboard.
2. Видит новые заявки (pending).
3. Подтверждает или отклоняет.
4. Управляет сотрудниками и пациентами.
5. Обновляет информацию клиники.

---

## 8. Безопасность

* Все запросы валидируются.
* Аутентификация через JWT.
* Пароли — bcrypt (saltRounds=10).
* Rate limit: 100 запросов/мин/пользователь.
* CSP и Helmet для защиты заголовков.
* Данные каждой клиники изолированы.

---

## 9. Тестирование

* Unit (Jest)
* Integration (Supertest)
* E2E (Cypress)
* Покрытие ≥ 70%

---

## 10. Документация

Обязательные файлы:

* `README.md` — запуск, структура
* `ARCHITECTURE.md` — описание модулей
* `API.md` — все маршруты
* `.env.example` — переменные окружения
* `RULES.md` — правила кодирования

---

## 11. Сроки и критерии завершения

**Сроки реализации MVP:** 6 недель
**Definition of Done:**

* Система развернута в облаке.
* Работает авторизация, CRUD, бронирование, лендинги.
* Multi-tenancy полностью функционирует.
* Все базовые тесты пройдены.