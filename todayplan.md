# 📋 Plan: Multi-Role Authentication System

**Дата:** 13 ноября 2025  
**Задача:** Реализация единой системы регистрации/входа с role-based доступом

---

## 🎯 Концепция (Вариант 1 - Выбран как лучший)

### Основная идея:
- ✅ **Единая страница Login** для всех ролей (admin, doctor, patient, partner)
- ✅ **Регистрация с выбором роли** - пользователь сам выбирает кем регистрируется
- ✅ **Auto-redirect** после входа на соответствующий dashboard
- ✅ **Role определяется backend'ом** из токена (безопасность)

---

## 👥 Роли в системе

1. **Patient (Пациент)** - обычный пользователь
2. **Doctor (Врач)** - специалист с лицензией
3. **Partner (Партнер)** - аптеки/лаборатории/организации
4. **Admin (Администратор)** - полный доступ к системе

---

## 📝 Этапы реализации

### **Этап 1: Database Schema** ✅
```
Обновить User model:
- Добавить enum role: [PATIENT, DOCTOR, PARTNER, ADMIN]
- Добавить enum status: [PENDING, ACTIVE, SUSPENDED, REJECTED]
- Добавить поля для каждой роли (specialty, organization, etc.)
```

### **Этап 2: Backend API**
```
1. Обновить регистрацию:
   POST /api/auth/register
   - Принимать role в body
   - Валидация по role (разные поля)
   - Status по умолчанию (PATIENT=ACTIVE, остальные=PENDING)

2. Обновить login:
   POST /api/auth/login
   - Возвращать role и status в токене
   - Проверка status (PENDING не может войти полностью)

3. Role-based endpoints:
   - /api/admin/* - только ADMIN
   - /api/doctor/* - только DOCTOR
   - /api/patient/* - только PATIENT
   - /api/partner/* - только PARTNER
```

### **Этап 3: Frontend - Registration**
```
1. Создать компонент RoleSelector:
   - Визуальный выбор роли (карточки/табы)
   - Icons для каждой роли
   
2. Динамическая форма регистрации:
   - Patient: базовые поля
   - Doctor: + specialty, license, experience
   - Partner: + organization, INN, address
   
3. Валидация на frontend по role
```

### **Этап 4: Frontend - Login (Unified)**
```
1. Одна страница /login для всех
2. Email/Phone + Password
3. После успешного входа:
   - Получить role из ответа
   - Redirect based on role:
     * PATIENT → /dashboard/patient
     * DOCTOR → /dashboard/doctor
     * PARTNER → /dashboard/partner
     * ADMIN → /dashboard/admin
```

### **Этап 5: Frontend - Role-based Dashboards**
```
1. Создать layouts для каждой роли:
   - PatientDashboard
   - DoctorDashboard
   - PartnerDashboard
   - AdminDashboard

2. Protected Routes с проверкой role

3. Разные sidebar меню для каждой роли
```

### **Этап 6: Approval System (для Doctor & Partner)**
```
1. Admin панель:
   - Список pending пользователей
   - Approve/Reject функционал
   
2. Email уведомления:
   - После регистрации (PENDING status)
   - После одобрения (ACTIVE status)
   - При отклонении (REJECTED status)
```

---

## 🔐 Важные решения

### Approval Logic:
- ✅ **PATIENT** → Instant ACTIVE (сразу может войти)
- ⏳ **DOCTOR** → PENDING → Admin approval → ACTIVE
- ⏳ **PARTNER** → PENDING → Admin approval → ACTIVE
- 🔒 **ADMIN** → Manual creation only (через database или super-admin)

### Registration Fields:

**Patient:**
- Имя, Фамилия
- Email/Телефон
- Пароль
- Дата рождения
- Пол

**Doctor:**
- Все поля Patient +
- Специальность (dropdown)
- Номер лицензии
- Опыт работы (лет)
- Клиника (optional)

**Partner:**
- Контактное лицо
- Название организации
- ИНН/ОГРН
- Адрес
- Тип партнера (аптека, лаборатория, страховая)

---

## 🎨 UX Flow

```
┌──────────────────────────────────────────────────┐
│              1. LANDING PAGE                     │
│                                                  │
│         [Login] [Register] Buttons               │
└──────────────────────────────────────────────────┘
                      │
        ┌─────────────┴─────────────┐
        │                           │
        ▼                           ▼
┌────────────┐            ┌──────────────┐
│   LOGIN    │            │  REGISTER    │
│            │            │              │
│ Email/Pass │            │ Choose Role: │
│            │            │ 👤 Patient   │
│  [Submit]  │            │ ⚕️ Doctor    │
└────────────┘            │ 🏢 Partner   │
        │                 │              │
        │                 │ [Form based  │
        │                 │  on role]    │
        │                 └──────────────┘
        │                         │
        └─────────┬───────────────┘
                  │
                  ▼ (Auto-redirect based on role)
        ┌─────────────────────────┐
        │   ROLE-BASED REDIRECT   │
        │                         │
        │ Patient  → /patient     │
        │ Doctor   → /doctor      │
        │ Partner  → /partner     │
        │ Admin    → /admin       │
        └─────────────────────────┘
```

---

## 🛠 Технические детали

### JWT Token Structure:
```json
{
  "userId": 123,
  "email": "user@example.com",
  "role": "DOCTOR",
  "status": "ACTIVE",
  "tenantId": 1
}
```

### Protected Route Logic:
```typescript
// Frontend
if (user.role !== requiredRole) {
  redirect('/unauthorized')
}

// Backend middleware
if (req.user.role !== 'ADMIN') {
  return res.status(403).json({ error: 'Forbidden' })
}
```

---

## ✅ Acceptance Criteria

- [ ] Регистрация работает для всех 3 ролей (Patient, Doctor, Partner)
- [ ] Login - единый для всех
- [ ] Auto-redirect работает корректно по role
- [ ] Patient получает instant access
- [ ] Doctor/Partner получают PENDING status
- [ ] Admin может approve/reject Doctor и Partner
- [ ] Каждая роль видит свой dashboard
- [ ] Protected routes работают корректно
- [ ] Email notifications отправляются

---

## 🚀 Порядок работы

1. **Сначала** - обновляем Database Schema (Prisma)
2. **Потом** - Backend API (controllers, services, validators)
3. **Затем** - Frontend Registration с role selector
4. **Далее** - Frontend role-based routing
5. **Наконец** - Admin approval system

---

## ⚠️ Риски и нюансы

1. **Migration существующих пользователей** - нужен будет migration script
2. **Email service** - нужен для уведомлений (можно использовать NodeMailer)
3. **Admin creation** - как создать первого admin? (через seed.js)
4. **Role change** - можно ли менять role после регистрации? (обычно нет)

---

## 📌 Следующие шаги

1. Получить подтверждение от пользователя ✅
2. Начать с Database Schema
3. Показывать прогресс после каждого этапа

---

**Статус:** Обсуждается  
**Приоритет:** Высокий  
**Оценка времени:** 4-6 часов work

